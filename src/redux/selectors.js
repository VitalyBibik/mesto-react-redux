export const cardsSelectors = (state) => state.cards;
export const usersSelectors = (state) => state.users;

export const cardsLoadedSelector = (state) => state.cards.loaded;
export const cardsLoadingSelector = (state) => state.cards.loading;
